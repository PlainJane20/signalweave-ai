# Offline mode is deterministic and needs no API key.
NORTHSTAR_PROVIDER=offline

# Optional hosted analysis. Never commit a real key.
OPENAI_API_KEY=
NORTHSTAR_OPENAI_MODEL=gpt-5.4-mini
NORTHSTAR_OPENAI_TIMEOUT_SECONDS=30
NORTHSTAR_OPENAI_MAX_RETRIES=2
NORTHSTAR_ALLOW_SENSITIVE_EXTERNAL=false
